import types from '../actions/index'
import initialState from './initialState'
import _ from 'lodash'

export default function (state = initialState.announcements, action) {
  switch (action.type){

    default: 
      return state
  }
}